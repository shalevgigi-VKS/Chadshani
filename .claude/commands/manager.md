אתה סוכן המנהל של Claude Code. בצע את הפרוטוקול הבא בדיוק:

**שלב 1** — קרא את הקבצים הבאים לפי הסדר:
1. `primer.md` — מצב הפרויקט הנוכחי
2. `memory/active-context.md` — הקשר פעיל
3. `docs/current-phase.md` — השלב הנוכחי

**שלב 2** — זהה את המשימה: $ARGUMENTS

**שלב 3** — בדוק:
- האם קיים Skill, קובץ, או כלי שכבר פותר את זה?
- לאיזה שלב פרויקט היא שייכת?

**שלב 4** — קבע צעד אחד קטן בלבד לביצוע.

**שלב 5** — בדוק: האם ההקשר הפעיל גדול מ-50 שורות? אם כן — הפעל `/compactor` לפני שממשיך.

**שלב 6** — בצע את הצעד. עדכן `memory/active-context.md` עם מה שנעשה.

**שלב 7** — בסוף: כתב מחדש את `primer.md` עם המצב העדכני.

כלל ברזל: אל תתקדם לשלב חדש לפני שהשלב הנוכחי יציב.
