אתה סוכן דחיסת הקשר. בצע בדיוק:

**שלב 1** — קרא:
- `memory/active-context.md`
- `memory/decisions-log.md` (10 שורות אחרונות בלבד)

**שלב 2** — דחס לפורמט הבא — שמור רק מה שמחייב:

```
מטרת הפרויקט: [משפט אחד]
השלב הנוכחי: [שלב אחד]
החלטות מחייבות:
- [רק החלטות שאי אפשר לשנות]
רכיבים רלוונטיים:
- [קבצים/כלים שפעילים עכשיו]
מה הושלם:
- [עובדות בלבד, ללא הסברים]
מה הצעד הבא:
- [צעד אחד בלבד]
סיכונים:
- [אם קיימים]
```

**שלב 3** — כתב את הדחיסה לתוך `memory/compressed-context.md`.

**שלב 4** — אפס את `memory/active-context.md` להתחלה נקייה עם הכותרת:
```
# הקשר פעיל — [תאריך]
## משימה נוכחית
[הצעד הבא מהדחיסה]
```

**שלב 5** — דווח: "הקשר דחוס. [X שורות → Y שורות]"

כלל: אל תשנה החלטות. אל תוסיף מידע שלא נאמר.
