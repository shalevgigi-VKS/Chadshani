rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{filename} {
      // Only authenticated users can read/write documents
      allow read, write: if request.auth != null;
    }
  }
}
