# Primer — מצב הפרויקט הנוכחי

> קובץ זה נכתב מחדש בסוף כל סשן על ידי קלוד.

מטרת הפרויקט: בניית מערכת Claude Code מלאה — חסכונית בטוקנים, מבוססת סוכנים, עם זיכרון קבוע בין סשנים.

השלב הנוכחי: שכבה 3 — סוכנים (שלבים 12-15)

החלטות מחייבות:
- CLI קודם לכל
- תמיד קטן ויציב — לא מתקדמים לפני יציבות
- Apify הוחלף ב-yt-dlp+whisper (חינמי, מקומי)
- סוכנים ממומשים כ-.claude/commands/ (לא כ-skills/)

מה הושלם:
- שכבה 0: CLAUDE.md (3 חלקים)
- שכבה 1: Gemini CLI, Whisper, youtube_transcribe.py
- שכבה 2: primer.md, 3 קבצי זיכרון, memory.sh, git+hook
- שכבה 3: /manager, /compactor כפקודות אמיתיות

מה הצעד הבא:
- שכבה 4 — שלב 16: Skill לכריית ידע מטקסט גדול

סיכונים:
- Gemini CLI דורש הגדרת API key לשימוש אמיתי
- whisper.exe לא ב-PATH (עובד דרך python -m whisper)
